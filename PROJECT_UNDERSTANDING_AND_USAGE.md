# AI Video Director Copilot 项目理解与使用整合建议

## 1. 这个项目本质上在做什么

这个项目不是一个“视频自动生成器”，更准确地说，它是一个 **面向视频创作前期与中期的 AI 导演工作台**。

它的核心链路是：

1. 你输入一个原始想法/选题/口播草稿
2. 系统先做一次意图分析（可选）
3. 再把内容转成结构化的分镜脚本 JSON
4. 然后根据脚本再生成一个“剪辑节奏方案”
5. 每个 scene 还可以继续生成 storyboard 图片

所以它解决的不是“最后成片渲染”，而是：

- 帮你更快从一个模糊想法走到可拍摄脚本
- 帮你提前想清楚每一段该拍什么
- 帮你减少“临场不知道拍啥”的卡顿
- 帮你在拍摄前就把 A-Roll / B-Roll / Screencast / Infographic 拆开

对你现在的目标来说，这个方向其实是对的：

> 你不是先追求一个复杂的 AI 工作流，而是要先快速指导自己，把本地想拍的内容尽快拍出来。

这个项目最有价值的地方，不是模型接口有多少，而是 **prompt 已经把“导演思维”和“视频结构思维”塞进去了**。

---

## 2. 项目的核心源码重点：确实主要在 prompt 设计

你说“源码主要是 prompt 的设计”，这个判断是对的。

从代码看，真正决定效果的核心不是 UI，而是这几层 prompt 结构：

### 2.1 输入层：项目创建输入
入口在 `components/ProjectInput.tsx:12`

用户会输入：

- `title`：项目标题
- `persona`：角色设定，比如 Tech Reviewer
- `rawContent`：原始内容
- `additionalPrompt`：补充要求
- `targetDuration`：目标时长
- `initialStyleId`：初始视觉风格

这里已经说明产品设计理念：

它不是让你一句话直接出结果，而是让你给“创作约束条件”。

这很适合你现在的场景，因为你本地想拍的视频，其实最需要的是：

- 主题是什么
- 给谁看
- 想拍几分钟
- 想要什么感觉
- 哪些地方必须出现

---

### 2.2 第一层 prompt：意图分析
默认 prompt 在 `constants.ts:4`
调用逻辑在 `services/gemini.ts:103`

这一层的作用不是直接写脚本，而是先把你的 raw input 提炼成：

- 核心信息
- 目标受众
- 信息层次
- 语气风格

这一步对“想到什么就先记下来”的工作方式很友好。

对于你这种“先把自己脑子里的东西快速变成拍摄指导”的目标，这一步是有帮助的。

但也有一个现实问题：

**如果你本来就已经很清楚自己要拍什么，这一步可能会增加一点流程成本。**

也就是说：

- 当你的输入很乱、很散时，开启它有价值
- 当你的输入已经接近拍摄提纲时，可以考虑关闭，直接生成脚本

这个开关在 `types.ts:133` 的 `enableIntentAnalysis`。

---

### 2.3 第二层 prompt：导演主 prompt
默认主 prompt 在 `constants.ts:17`
变量注入逻辑在 `services/gemini.ts:23`

这个项目最关键的设计在这里：

它不是一个单 prompt，而是一个 **主 prompt + 多个变量 prompt** 的拼装系统。

主 prompt 里有四个注入位：

- `{{DIRECTOR}}`
- `{{CINEMATOGRAPHY}}`
- `{{STORYBOARD}}`
- `{{CONTINUITY}}`

对应设置项定义在 `types.ts:147-152`，默认内容在 `constants.ts:41-65`。

这意味着它的真正能力不是“写分镜”，而是允许你把自己的创作方法拆成几个维度持续调教：

- 导演风格
- 镜头语言
- 分镜规则
- 连贯性规则

这部分就是整个项目最值得保留和继续发展的资产。

---

### 2.4 第三层 prompt：脚本生成 prompt
脚本 prompt 在 `utils/promptBuilder.ts:4`

这里把输入内容转成要求严格的 JSON，要求字段包括：

- scene type
- duration
- script
- emotion
- visual_spec
- image_prompt
- editing_note

而且它很强调几个规则：

- 不能全是 talking head
- 软件/网站/数字流程必须优先变成 Screencast
- 抽象内容适合 Infographic
- 需要 A-Roll 和其他视觉类型交替

这很适合做“可执行拍摄计划”，因为它在强迫模型输出可操作的镜头单位，而不是一段泛泛的文案。

对你来说，最有用的不是它有没有把文案写得多花，而是它已经在替你回答：

- 这一句应该口播还是画面展示？
- 这里该录屏还是拍人？
- 这里适不适合做插图或信息卡？

---

### 2.5 第四层 prompt：剪辑方案 prompt
默认编辑 prompt 在 `constants.ts:67`
具体构建在 `utils/promptBuilder.ts:55`
调用在 `store/AppContext.tsx:328`

这一层会根据线性脚本生成 timeline，动作包括：

- `KEEP`
- `TRIM`
- `MOVE_TO_START`
- `REORDER`

它的目标不是还原剧本，而是给“短视频 retention”做优化。

这意味着它更像一个：

- 剪辑前预演器
- 节奏审查器
- 钩子强化器

如果你的目标是快速拍自己要发的视频，这个模块可以帮你提前判断：

- 开头钩子够不够强
- 哪些片段会显得拖
- 哪些内容适合挪到前面

---

## 3. 项目的实际运行链路

从 `store/AppContext.tsx:264` 可以看到完整流程：

### 创建项目时
1. 可选：先做意图分析
2. 生成 script JSON
3. 保存到本地 IndexedDB
4. 可选：为每个 scene 自动生成图片

### 后续可做的事
- 在 `ScriptBoard` 看 scene 列表：`components/ScriptBoard.tsx:9`
- 在 `EditingTimeline` 生成剪辑建议：`components/EditingTimeline.tsx:13`
- 在设置页改 prompt：`components/SettingsView.tsx:14`、`components/settings/PromptSettings.tsx:24`

本地存储在 IndexedDB，逻辑在 `services/db.ts:18`。

也就是说，这个项目当前是一个 **本地创作工作台**，不是云端协作系统。

这点其实符合你当前阶段：先把自己的内容拍出来，比先做复杂协同重要得多。

---

## 4. 这个项目当前最适合你的用法

结合你的目标，我觉得这个项目最适合被定义成：

## “个人拍摄前置指挥台”

不是“全能视频平台”，而是你每次开拍前的快速准备器。

建议你把它当成下面这套流程使用：

### Step 1：先只输入最原始的拍摄想法
比如：

- 今天想拍什么
- 想表达什么观点
- 有没有一个真实场景
- 有没有必须展示的产品/页面/动作
- 最终想发在哪个平台

rawContent 不需要一开始就写得很工整。

你更适合写成这种：

- 我今天想拍一个 AI 工具实操
- 核心是告诉别人它不是玄学，3 步就能跑起来
- 我自己会出镜，但不能全程出镜
- 要多一些录屏
- 视频控制在 60-90 秒
- 开头 3 秒必须直接把结果拿出来

这个输入方式和当前项目是匹配的。

---

### Step 2：把 prompt 调成“服务于你自己拍摄”而不是“泛创意生成”

现在默认 prompt 偏“专业导演工具”。方向没错，但对“快速拍自己的视频”来说，还是偏泛、偏重。

你真正需要的 prompt 更像是：

- 优先生成可拍、可执行、少废镜头的脚本
- 优先考虑一个人就能完成的镜头
- 优先用手机、本地环境、屏幕录制能完成的拍摄方案
- 少给大场景和复杂运动镜头
- 多给“拍摄动作指令”

也就是说，你后面要改的重点不是 UI，而是这几个 prompt 维度：

1. `{{STORYBOARD}}`：强化“一个人也能拍”
2. `{{CINEMATOGRAPHY}}`：强化“手机/桌面/固定机位友好”
3. `{{DIRECTOR}}`：强化“口语化、短句、能直接讲”
4. `editingPrompt`：强化“适合短视频首发”

---

### Step 3：把产出当“拍摄清单”而不是“最终答案”

这个项目输出 JSON 很结构化，所以你不要把它看成一篇完整脚本，而要把它看成：

- 每个 scene 是一个待拍镜头
- `script` 是口播/字幕内容
- `visual_spec.description` 是你该拍什么
- `editing_note` 是后期提示
- `image_prompt` 是辅助你理解画面

对你实际拍摄最有帮助的，是把 scene 直接转成：

- 先拍哪些口播
- 再补哪些 B-roll
- 哪些一定要录屏
- 哪些镜头可以不拍，后面用图文替代

---

## 5. 我对项目能力的判断

## 优势

### 5.1 prompt 分层做得对
这是项目最有价值的地方。

不是一个超长 prompt 写死，而是把：

- 意图分析
- 导演 prompt
- 变量 prompt
- 剪辑 prompt

拆成了可配置层。

这让它很适合逐步调成你的私人工作流。

### 5.2 输出结构清晰
`types.ts:38-95` 的数据结构很完整。

这带来的好处是：

- 适合继续加工
- 适合后续接拍摄清单
- 适合接自动导出
- 适合未来接 Notion / 飞书 / markdown 工作流

### 5.3 已经考虑了多模型接入
`services/gemini.ts:43` 和 `types.ts:103` 支持多个 provider。

虽然你当前最该关注的不是 provider 数量，但这给后续调整成本留了空间。

---

## 6. 当前项目对你来说的问题

## 6.1 现在更像“AI 创意导演台”，还不够像“个人拍摄助手”

这就是你说“用起来还是稍微麻烦一点”的根源。

因为它现在默认在解决：

- 怎么生成更专业的分镜
- 怎么生成更电影感的镜头
- 怎么做编辑节奏优化

但你眼下更关心的是：

- 我今天到底该怎么快速拍出来
- 我一个人先拍哪些镜头
- 哪些镜头能省
- 哪些地方必须录屏
- 怎么最短路径发出视频

也就是说，当前产品目标和你的使用目标之间，差了一个“落地执行层”。

---

## 6.2 输入成本还是偏高

现在创建项目要填：

- 标题
- persona
- 时长
- 原始内容
- 补充 prompt
- 风格

对于认真做项目没问题，但对于你这种“临时想到一个视频就想赶紧整理然后去拍”的场景，步骤还是稍重。

你真正高频使用时，可能更想要的是：

- 一个超简输入框
- 输入今天想拍什么
- 选择模板
- 直接生成“拍摄版脚本”

---

## 6.3 输出还是偏“创作中间态”，没有完全变成执行清单

目前 scene 很完整，但还不够直接指导拍摄动作。

例如缺少这种更落地的字段：

- 是否需要出镜
- 是否需要录屏
- 拍摄地点
- 拍摄设备建议
- 优先级
- 是否可跳过
- 是否可以后期补

所以它现在更像“导演分镜数据”，不是“你的今日拍摄清单”。

---

## 7. 最适合你的整合方向

我建议不是大改，而是分两层理解：

## 第一层：马上可用的整合方式
先不大动架构，直接把它当成：

### 模式 A：选题变拍摄脚本
输入一个想法，输出：
- 分 scene 的口播与镜头
- 哪些要录屏
- 哪些用 B-roll
- 剪辑顺序建议

### 模式 B：已有草稿变分镜
你已经有一段文案/提纲，丢进去，让它拆成：
- 开头钩子
- 主体演示
- 结尾收束
- 补镜头清单

### 模式 C：产品实拍 / AI 工具演示视频
这会是你最匹配的一类。
因为项目对 `Screencast` 的规则已经比较明确，适合：
- 产品 walkthrough
- 工具教程
- 工作流展示
- 页面操作演示

---

## 第二层：下一步应该强化的能力

如果要更贴合你的个人使用，我建议未来沿着下面 4 个方向优化：

### 方向 1：增加“快速拍摄模板”
不是让用户自由发挥 prompt，而是预设几种模式：

- 口播观点类
- 工具演示类
- 产品实拍类
- 教程拆解类
- Vlog/记录类

这样你每次不需要重新思考 prompt。

### 方向 2：增加“拍摄执行字段”
在 scene 结构上再加一层执行信息，比如：

- shoot_mode: talking / broll / screen / overlay
- location: desk / room / outdoor / screen
- equipment: phone / tripod / screen-recording
- priority: must / should / optional

这样它就能直接从“分镜 JSON”升级成“拍摄清单 JSON”。

### 方向 3：增加“一人创作约束”
你的场景很明显是偏个人创作，因此 prompt 应明确限制：

- 默认单人完成
- 默认室内可拍
- 默认低成本设备
- 默认不依赖团队协作
- 默认先出可发布版本，而非追求满配镜头

### 方向 4：把输出再加工成可执行文档
目前系统能输出 JSON，但对你来说最好的是再自动整理成：

- 拍摄顺序表
- 镜头 checklist
- 口播提词版
- 后期素材 checklist

这一步是最值得加的，因为它直接决定你会不会每天真用。

---

## 8. 一个更适合你的实际使用方法

如果今天就开始用，我建议你这样操作：

## 日常工作流（建议版）

### 1）先只写 5 行原始输入
模板可以固定成：

- 我要拍什么
- 这条视频想让观众知道什么
- 要展示什么页面/动作/结果
- 我是否出镜
- 目标时长

### 2）先关闭复杂度高但不必要的项
在你还没形成固定节奏前：

- 可以先保留意图分析，但如果觉得慢就关掉
- 图片自动生成可以先关掉
- 先专注脚本和剪辑方案

### 3）先用 ScriptBoard 判断“是否能拍”
不是看它写得美不美，而是看：

- scene 是否过多
- 是否太多 A-Roll
- Screencast 是否足够明确
- 有没有你拍不了的镜头

### 4）再用 EditingTimeline 判断“怎么发更快”
重点看：

- 哪一段应该开头先放
- 哪些 scene 可以缩短
- 哪些 scene 没必要拍满

### 5）最后你自己人工改一轮 scene
这个项目的最佳用法不是“一键生成就拍”，而是：

- AI 先给结构
- 你把不可执行的 scene 改掉
- 再按自己现实条件拍

---

## 9. 我建议你现在先做的，不是全面重构

如果我们接下来要继续整合使用，我建议优先级如下：

### 第一优先级
先把 prompt 调成 **“个人快速拍摄模式”**。

也就是先改：
- director 主 prompt
- storyboard 变量
- cinematography 变量
- editing prompt

目标是让输出默认更偏：

- 可执行
- 单人可拍
- 本地场景可拍
- 录屏友好
- 短视频友好

### 第二优先级
把 JSON 再整理成一份“拍摄执行文档”。

这个文档可以直接输出：
- 开拍顺序
- 每个镜头要干什么
- 哪些要出镜
- 哪些要录屏
- 哪些是可选镜头

### 第三优先级
再考虑 UI 层面的“快速模式”。

比如：
- 一个极速创建入口
- 几个预设模板
- 一键输出拍摄清单

---

## 10. 我给你的结论

一句话总结：

**这个项目的核心价值是“可配置 prompt 驱动的导演工作流”，而你最适合把它收敛成“个人视频拍摄前置助手”。**

也就是说，不要把它继续做重，不要先追求大而全。

你现在最值得做的是：

1. 保留 prompt 分层架构
2. 把 prompt 调成更适合你个人拍摄
3. 把输出从“分镜 JSON”进一步变成“拍摄执行单”
4. 让你每次想到内容时能在几分钟内得到可拍方案

---

## 11. 我建议的下一步协作方式

如果你愿意，我下一步可以直接帮你做两件事里的一个：

### 方案 A：先只做文档和方法
我继续基于这个项目，帮你输出一份：

- 适合你个人使用的 prompt 改造方案
- 适合“本地快速拍视频”的输入模板
- 一套标准工作流

### 方案 B：直接开始改项目
我直接帮你在代码里落地第一版“个人快速拍摄模式”，优先改：

- 默认 prompts
- 项目输入文案
- 输出结构或导出文档

如果按实用性排序，我建议先做 **方案 A + 一点点方案 B**：

先把你的个人工作流定义清楚，再把 prompt 和输出改成真正适合你每天用的版本。
